# 컴파일룸 (Compile-Room)

전주대학교 컴퓨터공학과 학생들을 위한 통합 자료실, 게시판, 실시간 채팅 서비스입니다. Next.js 16(App Router), Prisma, NextAuth를 기반으로 구현되었으며, 모든 문서는 한국어로 작성되었습니다.

---

### 왜 컴파일룸을 만들었나요?

- **파편화된 정보 통합**: 전공 자료, 스터디 공지, 졸업 정보 등이 여러 플랫폼에 흩어져 있어 정보 접근성이 떨어지는 문제를 해결하고자 했습니다.
- **지식 공유의 장 마련**: 실습이나 캡스톤 디자인을 통해 얻은 노하우가 후배들에게 체계적으로 전달되지 못하는 아쉬움을 해소하고 싶었습니다.
- **개발 환경의 일원화**: macOS와 Ubuntu 등 어떤 환경에서든 동일하게 개발하고 운영할 수 있는 표준화된 플랫폼을 구축하는 것을 목표로 삼았습니다.

---

### ✨ 핵심 기능

- **통합 대시보드**: 최신 및 인기 자료를 실시간으로 확인하고, 검색 기능을 통해 원하는 정보를 빠르게 찾을 수 있습니다.
- **카테고리별 게시판**: 전공, 교양, 공지, 취업 등 다양한 카테고리별로 게시물을 필터링하고, 조회수 통계를 제공합니다.
- **자료 업로드 및 관리**: 로그인한 사용자는 PDF, PNG, JPG 형식의 파일을 최대 3MB까지 업로드할 수 있습니다.
- **실시간 채팅 시스템**: 다중 채팅방 생성, 비밀번호를 이용한 비공개 채팅, 방장 권한 등 다양한 기능을 지원합니다. 모든 메시지는 AES-256-GCM으로 암호화되어 안전하게 보호됩니다.
- **온보딩 및 동기 부여**: 서비스 최초 접속 시 온보딩 가이드를 제공하며, 종강 및 개강까지의 카운트다운 기능으로 학사 일정에 대한 동기를 부여합니다.
- **학과 정보 허브**: 수강 편람, 학칙 등 학과 관련 주요 정보를 제공하고, 프로필에 설정된 학년에 따라 맞춤형 콘텐츠를 추천합니다.
- **강화된 계정 관리**: 안전한 비밀번호 정책을 적용했으며, 프로필 페이지에서 개인 정보 수정 및 계정 탈퇴 기능을 제공합니다.
- **손쉬운 접근성**: 서비스 소개, 기여자 정보, 오류 제보 등 주요 기능에 쉽게 접근할 수 있도록 UI를 구성했습니다.

---

### 👤 체험용 계정

| 이름 | 이메일 | 비밀번호 |
| --- | --- | --- |
| 김현민 | `hyunmin@compileroom.com` | `compileroom123` |
| 최준혁 | `junhyeok@compileroom.com` | `compileroom123` |
| 이은우 | `eunwoo@compileroom.com` | `compileroom123` |
| 정찬호 | `chanho@compileroom.com` | `compileroom123` |

> 모든 계정은 기본 학년으로 설정되어 있으며, 로그인 후 프로필 페이지에서 자유롭게 학년과 학번을 수정할 수 있습니다.

---

### 🔐 운영 및 보안 정책

- **메시지 암호화**: 채팅 메시지는 PBKDF2와 Salt를 이용해 생성된 키로 AES-256-GCM 방식의 종단간 암호화를 거쳐 안전하게 저장됩니다.
- **익명성 보장**: 익명 채팅 시에는 IP와 사용자 ID를 해시하여 `익명#1234`와 같은 고유 닉네임을 부여합니다.
- **업로드 제한**: 보안을 위해 3MB 이하의 PDF, PNG, JPG 파일만 업로드를 허용하며, 파일명은 정규화하여 저장합니다.
- **비용 정책**: 별도의 유료 API나 외부 서비스를 사용하지 않아 추가적인 과금 요소가 없습니다.
- **요청 제한 (Rate Limit)**: 안정적인 서비스 운영을 위해 메모리 기반의 요청 제한(Rate Limit)을 적용하여 비정상적인 트래픽을 차단합니다.

---

### 🗂️ 프로젝트 구조

```
src/
 ├─ app/                     # App Router 기반 페이지 및 API 라우트
 │   ├─ api/                 # 인증, 업로드, 게시판, 채팅 등 Route Handler
 │   ├─ chat, posts, upload  # 핵심 기능 페이지
 │   └─ department           # 학과 정보 허브 페이지
 ├─ components/              # Header, Form, ChatRoom 등 UI 컴포넌트
 ├─ data/department.ts       # 학과 및 교과 과정 정적 데이터
 ├─ lib/                     # Prisma, Auth, Crypto 등 핵심 로직
 └─ utils/alias.ts           # 익명 닉네임 생성 유틸리티
prisma/
 ├─ schema.prisma            # 데이터베이스 스키마
 └─ migrations/              # SQLite 마이그레이션 기록
scripts/
 ├─ seed.mjs                 # 테스트용 초기 데이터 생성
 ├─ setup-local-https.sh     # 로컬 HTTPS 환경 설정
 └─ deploy.sh                # 서버 배포 자동화 스크립트
public/compileroom-logo.png  # 파비콘 및 사이트 로고
```

> `구상도/` 디렉토리와 모든 인증서(`*.pem`) 파일은 `.gitignore`에 등록되어 원격 저장소에 포함되지 않습니다.

---

### 📁 주요 폴더 및 파일 역할

| 경로 | 설명 |
| --- | --- |
| `src/app/layout.tsx` | 공통 레이아웃, 파비콘, 폰트, 메타 태그 정의 |
| `src/app/page.tsx` | 메인 대시보드, 검색, 카운트다운, 통계 기능 |
| `src/app/chat/*` | 실시간 채팅 페이지 및 관련 API |
| `src/app/posts/*` | 게시판 목록, 상세 페이지, 조회수 API |
| `src/app/department/*` | 학년별 맞춤 커리큘럼 및 학칙 정보 제공 |
| `src/components/chat-room.tsx` | SWR 기반 실시간 채팅 UI |
| `src/components/countdown-timer.tsx` | D-Day 카운트다운 및 동기 부여 메시지 |
| `src/lib/crypto.ts` | AES-256-GCM 기반 채팅 메시지 암복호화 로직 |
| `scripts/seed.mjs` | 초기 데이터(계정, 게시글, 채팅) 생성 |
| `scripts/setup-local-https.sh` | `mkcert`를 이용한 로컬 HTTPS 인증서 설정 |
| `scripts/deploy.sh` | `npm ci`, `migrate`, `build`, `pm2`를 순차적으로 실행하는 배포 자동화 |
| `package.json` | `dev`, `seed`, `deploy` 등 주요 스크립트 정의 |

---

### ⚙️ 환경 변수 (.env)

| 키 | 설명 |
| --- | --- |
| `DATABASE_URL` | 데이터베이스 연결 URL. 기본값은 `file:./dev.db` (SQLite)입니다. |
| `AUTH_SECRET` | NextAuth 세션 및 암호화에 사용되는 비밀 키입니다. |
| `NEXTAUTH_URL` | 배포된 서비스의 URL입니다. (예: `https://compileroom.space`) |
| `CHAT_ENCRYPTION_KEY` | 채팅 암복호화에 사용되는 키입니다. (미설정 시 `AUTH_SECRET` 사용) |

---

### 🧪 로컬 개발 환경 설정

1.  **의존성 설치 및 데이터베이스 설정**
    ```bash
    npm install
    npx prisma migrate dev
    npm run seed # 테스트용 데이터 생성
    ```

2.  **개발 서버 실행**
    ```bash
    npm run dev # http://localhost:3000
    ```

3.  **빌드 테스트**
    ```bash
    npm run build # Turbopack 대신 Webpack으로 빌드하여 호환성 확보
    ```

---

### ⚡️ 주요 스크립트

| 명령어 | 설명 |
| --- | --- |
| `npm run dev` | HTTP 환경에서 개발 서버를 실행합니다. |
| `npm run dev:https` | `mkcert`를 이용해 HTTPS 개발 서버를 실행합니다. |
| `npm run seed` | `scripts/seed.mjs`를 실행하여 초기 데이터를 생성합니다. |
| `npm run deploy` | `scripts/deploy.sh`를 실행하여 서버에 배포합니다. |

> `git`, `node 20`, `npm`, `pm2`, `mkcert` 등 필수 도구는 가이드를 따라 한 번만 설치하면, 이후 모든 과정을 스크립트 명령어로 간편하게 처리할 수 있습니다.

---

### 🔐 로컬 HTTPS 환경 구성 (mkcert)

`mkcert`를 사용하여 로컬 환경에서 HTTPS를 구성할 수 있습니다.

1.  **mkcert 설치**
    ```bash
    # macOS
    brew install mkcert
    brew install nss

    # Ubuntu
    sudo apt install libnss3-tools
    ```

2.  **HTTPS 개발 서버 실행**
    ```bash
    npm run dev:https
    ```
    - `scripts/setup-local-https.sh` 스크립트가 인증서(`certs/localhost.pem`, `localhost-key.pem`)를 자동으로 생성합니다.
    - `mkcert`가 설치되지 않은 경우, 스크립트가 자동으로 설치를 시도합니다.

---

### 🚀 서버 배포 가이드 (Ubuntu)

1.  **필수 패키지 설치**
    ```bash
    sudo apt update
    sudo apt install -y curl git build-essential
    curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
    sudo apt install -y nodejs
    sudo npm install -g pm2
    ```

2.  **프로젝트 클론 및 설정**
    ```bash
    cd /var/www
    git clone <YOUR_REPO_URL> compile-room
    cd compile-room
    cp .env.example .env # 또는 직접 .env 파일 작성
    ```

3.  **원터치 배포 스크립트 실행**
    ```bash
    npm run deploy
    ```
    - `scripts/deploy.sh` 스크립트가 `npm ci`, `prisma migrate deploy`, `npm run build`, `pm2 restart/start`를 순차적으로 실행합니다.

4.  **HTTPS 설정 (Let's Encrypt & Nginx)**
    - `Certbot`을 사용하면 무료로 SSL 인증서를 발급받고 자동으로 갱신할 수 있습니다.
      ```bash
      sudo apt install -y nginx certbot python3-certbot-nginx
      sudo certbot --nginx -d compileroom.space -d www.compileroom.space
      ```
    - Nginx 리버스 프록시 설정 예시 (`/etc/nginx/sites-available/compileroom`):
      ```nginx
      server {
        server_name compileroom.space www.compileroom.space;

        location / {
          proxy_pass http://127.0.0.1:3000;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
      }
      ```

5.  **파일 업로드 경로 확인**
    - 업로드된 파일은 기본적으로 `public/uploads/`에 저장되며, 이 경로는 `.gitignore`에 포함되어 있습니다.
    - 운영 환경에서는 NFS나 S3 같은 외부 스토리지를 사용하도록 API를 수정하거나 심볼릭 링크를 사용하는 것을 권장합니다.

---

### ✅ 주요 기능 체크리스트

- [ ] `npm run build`가 성공적으로 완료되는지 확인
- [ ] `/upload` 페이지에서 3MB 이하의 PDF, PNG, JPG 파일 업로드 및 미리보기 확인
- [ ] `/posts` 페이지에서 게시글 작성, 조회수, 상대 시간, `(예제)` 배지 표시 확인
- [ ] `/chat` 페이지에서 로그인/익명 전환, 채팅방 생성 및 입장, 요청 제한(Rate Limit) 기능 확인
- [ ] `/department` 페이지에서 학년 변경 시 프로필과 동기화되는지 확인
- [ ] `/about` 페이지의 프로젝트 소개, 기여자, 문의 링크가 정상적으로 동작하는지 확인

---

### 📬 문의

- **이메일**: `mini0227kim@gmail.com`

> 이 문서를 통해 로컬 개발 환경 설정부터 실제 서버 배포까지의 모든 과정을 쉽게 따라 할 수 있습니다. 추가 비용 없이 자유롭게 기능을 확장하고 개선해보세요.
