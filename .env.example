# 환경별 설정값 예시 (이 파일을 복사해 .env 로 사용하세요)

# Prisma DB 연결 (로컬: SQLite)
DATABASE_URL="file:./dev.db"

# NextAuth 서명 키 (32자 이상 임의의 난수)
AUTH_SECRET="please_change_me_to_random_hex"

# 배포 URL (로컬: http://localhost:3000, 운영: https://compileroom.space)
NEXTAUTH_URL="http://localhost:3000"

# 채팅 암호화 키 (선택. 미지정 시 AUTH_SECRET 사용)
# CHAT_ENCRYPTION_KEY="another_random_secret"
